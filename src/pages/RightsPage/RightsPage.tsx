import { Typography } from '../../components/base/Typography';

export const RightsPage = () => {
  return (
    <div style={{ padding: '80px 0', textAlign: 'center' }}>
      <Typography variant="h2">Rights</Typography>
      <br></br>
      <Typography variant="body">Information will be added later.</Typography>
    </div>
  );
};
